# Node
node_modules/
npm-debug.log*
*.tsbuildinfo

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Secrets — never commit
.env
.env.local
*.pem
*.key

# TrueForge local state
.trueforge/
*.sqlite
*.sqlite-journal

# Playwright / browser profiles
.browser-profile/
playwright-report/
test-results/
# Page snapshots the MCP server writes as it works. Never commit these:
# once we point the agent at LinkedIn they contain real feed content.
.playwright-mcp/

# Runtime artifacts
logs/
*.log
audio_tmp/
